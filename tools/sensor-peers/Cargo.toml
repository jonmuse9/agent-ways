[package]
name = "sensor-peers"
version = "0.6.0"
edition = "2021"
description = "discovers Claude Code sessions and reads peer signals"
license = "MIT"

[dependencies]
sensor-trait = { path = "../sensor-trait" }
serde_json = "1"
