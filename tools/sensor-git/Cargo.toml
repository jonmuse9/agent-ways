[package]
name = "sensor-git"
version = "0.6.0"
edition = "2021"
description = "tracks dirty files, branch changes, and upstream divergence"
license = "MIT"

[dependencies]
sensor-trait = { path = "../sensor-trait" }
