---
description: project local plugin first enforcement build features as plugins
vocabulary: plugin first enforcement extension module register hook capability project local
pattern: (?i)zqxplugintest
scope: agent
refire: 0.15
---
# Plugin First enforcement

Build new features as plugins that register against the host. Prefer the
plugin/extension surface over editing the core.
