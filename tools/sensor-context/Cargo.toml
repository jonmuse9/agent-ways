[package]
name = "sensor-context"
version = "0.6.0"
edition = "2021"
description = "tracks context window usage and projects compaction"
license = "MIT"

[dependencies]
sensor-trait = { path = "../sensor-trait" }
