name: Build ways CLI

on:
  push:
    branches: [main]
    tags: ['ways-v*']
    paths:
      - 'tools/ways-cli/**'
      - 'tools/agent-fmt/**'
      - 'tools/Cargo.toml'
      - 'tools/Cargo.lock'
      - '.github/workflows/build-ways.yml'
  pull_request:
    paths:
      - 'tools/ways-cli/**'
      - 'tools/agent-fmt/**'
      - 'tools/Cargo.toml'
      - 'tools/Cargo.lock'
      - '.github/workflows/build-ways.yml'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            platform: linux-x86_64
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            platform: linux-aarch64
            os: ubuntu-latest
          - target: x86_64-apple-darwin
            platform: darwin-x86_64
            os: macos-latest
          - target: aarch64-apple-darwin
            platform: darwin-arm64
            os: macos-latest

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cargo-zigbuild (Linux cross-compile)
        if: runner.os == 'Linux' && matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          pip3 install ziglang
          cargo install cargo-zigbuild

      - name: Build (native)
        if: "!(runner.os == 'Linux' && matrix.target == 'aarch64-unknown-linux-gnu')"
        run: cargo build --release --manifest-path tools/Cargo.toml -p ways --target ${{ matrix.target }}

      - name: Build (zigbuild cross-compile)
        if: runner.os == 'Linux' && matrix.target == 'aarch64-unknown-linux-gnu'
        run: cargo zigbuild --release --manifest-path tools/Cargo.toml -p ways --target ${{ matrix.target }}

      - name: Package binary
        run: |
          mkdir -p dist
          cp tools/target/${{ matrix.target }}/release/ways dist/ways-${{ matrix.platform }}

      # Native builds can run directly; cross-compiled binaries can only be verified with `file`
      - name: Smoke test
        if: "!(matrix.target == 'aarch64-unknown-linux-gnu' && runner.os == 'Linux') && !(matrix.target == 'x86_64-apple-darwin' && runner.arch == 'ARM64')"
        run: dist/ways-${{ matrix.platform }} --version

      - name: Verify binary (cross-compiled — can't execute on this runner)
        if: "(matrix.target == 'aarch64-unknown-linux-gnu' && runner.os == 'Linux') || (matrix.target == 'x86_64-apple-darwin' && runner.arch == 'ARM64')"
        run: |
          file dist/ways-${{ matrix.platform }}
          ls -lh dist/ways-${{ matrix.platform }}

      - uses: actions/upload-artifact@v5
        with:
          name: ways-${{ matrix.platform }}
          path: dist/ways-${{ matrix.platform }}

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - name: Run unit tests
        run: cargo test --manifest-path tools/Cargo.toml -p ways
      - name: Run simulation tests
        run: cargo test --manifest-path tools/Cargo.toml -p ways --test session_sim -- --test-threads=1

  collect:
    needs: [build, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: binaries
          merge-multiple: true

      - name: Generate checksums
        run: |
          cd binaries
          sha256sum ways-* > checksums.txt
          cat checksums.txt
          ls -lh

      - uses: actions/upload-artifact@v5
        with:
          name: ways-all-platforms
          path: binaries/

  release:
    needs: collect
    if: startsWith(github.ref, 'refs/tags/ways-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v5
        with:
          name: ways-all-platforms
          path: binaries

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          gh release create "$TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --title "ways ${TAG#ways-}" \
            --notes "Pre-built ways CLI binaries for 4 platforms.

          Install: \`bash tools/ways-cli/download-ways.sh\`
          Or build from source: \`make ways\`" \
            binaries/*
